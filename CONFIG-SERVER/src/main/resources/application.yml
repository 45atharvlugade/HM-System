server:
  port: 8888

spring:
  application:
    name: CONFIG SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/45atharvlugade/HM-System.git
          username: 45atharvlugade
          clone-on-start: true

logging:
  level:
    org.springframework.cloud.config: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: "*"